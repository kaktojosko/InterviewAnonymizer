

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
      - ./tmp:/app/tmp
      # Optional: mount src for hot-reloading in dev
      # - ./src:/app/src
    restart: unless-stopped
